# Training

The bearing degradation demo uses a deterministic health-index model (`bearing-degradation-v1`) driven by simulated vibration/temperature drift. No offline training job is required for `make one-shot`.
